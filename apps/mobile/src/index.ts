// @wfx/app-mobile — Lane C client surface (WFX-040).
//
// The mobile entry (`src/main.ts`) boots the shared cross-platform client
// runtime (from `@wfx/app-web`) on the Mobile capability profile with
// injected OS background inputs — the OS-constrained client.

export * from "./main";
