// @wfx/app-desktop — Lane C client surface (WFX-040).
//
// The desktop entry (`src/main.ts`) boots the shared cross-platform client
// runtime (from `@wfx/app-web`) on the Desktop capability profile with a
// native media engine port bound — the reference full-power client.

export * from "./main";
