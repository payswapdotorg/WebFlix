bun test v1.3.14 (0d9b296a)

apps/desktop/tests/r27-parity-tokens.test.ts:
(pass) R27-W3 the token contract (the corpus sheet, typed) > mirrors EVERY sheet row's dark and light values exactly [0.15ms]
(pass) R27-W3 the token contract (the corpus sheet, typed) > every token carries its custom-property name + provenance + sheet row [0.22ms]
(pass) R27-W3 the token contract (the corpus sheet, typed) > resolves by theme, dark DEFAULT (the resolver is the only read path) [0.13ms]
(pass) R27-W3 the token contract (the corpus sheet, typed) > is frozen — mutation attempts fail (the immutable-sheet law) [0.06ms]
(pass) R27-W3 the token contract (the corpus sheet, typed) > the typography ladder mirrors the sheet's roles [0.09ms]
(pass) R27-W3 the token contract (the corpus sheet, typed) > r27TypeStyle composes the role with the theme's color [0.05ms]
(pass) R27-W3 the token contract (the corpus sheet, typed) > the geometry contract mirrors the measured/documented values [0.10ms]
(pass) R27-W3 the token contract (the corpus sheet, typed) > the motion contract pins the sheet's band values [0.03ms]
(pass) R27-W3 the generated stylesheet (the single encoding) > dark rides :root (the DEFAULT); light rides the data-theme seam [0.14ms]
(pass) R27-W3 the generated stylesheet (the single encoding) > emits every token's custom property in both themes [0.74ms]
(pass) R27-W3 the generated stylesheet (the single encoding) > carries the corpus geometry verbatim (the grammar's skeleton) [0.25ms]
(pass) R27-W3 the generated stylesheet (the single encoding) > emits no hand-authored theme colors — every color rides a var() [0.53ms]
(pass) R27-W3 the generated stylesheet (the single encoding) > grammar-identity classes exist for every corpus surface [0.33ms]

apps/desktop/tests/r27-chrome-surface.test.ts:
(pass) R27-W3 the chrome control bar (the corpus order) > renders the corpus order, left→right [0.18ms]
(pass) R27-W3 the chrome control bar (the corpus order) > next renders ONLY when the session queue holds an entry (the corpus law) [0.08ms]
(pass) R27-W3 the chrome control bar (the corpus order) > volume answers the honest per-rung truth (native: not-exposed-yet; embed: realization-exposed) [0.08ms]
(pass) R27-W3 the chrome control bar (the corpus order) > the time readout carries the honest numbers (cur / dur; cur-only while unknown) [0.22ms]
(pass) R27-W3 the scrub (the red #f03 grammar over the truthful runway) > pins the corpus geometry + the red fill + the honest fractions [0.11ms]
(pass) R27-W3 the scrub (the red #f03 grammar over the truthful runway) > the verified-copy demotion law stays UNCHANGED underneath (R26) [0.08ms]
(pass) R27-W3 the idle fade (the corpus motion) > fades at ~3s idle; mousemove/focus reveals; the fade is 300ms [0.04ms]
(pass) R27-W3 the keyboard map (the corpus retarget: t = theater) > binds the corpus keys [0.23ms]
(pass) R27-W3 the keyboard map (the corpus retarget: t = theater) > parses events; unbound keys and modified chords answer null [0.09ms]
(pass) R27-W3 the settings popup (two-level; the capability rows live inside) > carries EVERY WebFlix capability row — gated on truthful availability, never removed [0.33ms]
(pass) R27-W3 the settings popup (two-level; the capability rows live inside) > the gates flip with the truth (capability truth BOTH directions) [0.09ms]
(pass) R27-W3 the settings popup (two-level; the capability rows live inside) > the two-level navigation: speed/quality submenus + the back-arrow pop [0.13ms]
(pass) R27-W3 the settings popup (two-level; the capability rows live inside) > the quality submenu answers the honest per-rung truth [0.08ms]
(pass) R27-W3 the stage modes (the corpus stage law) > default: 16:9 radius 12; theater/fullscreen: full-bleed radius 0 [0.04ms]

apps/desktop/tests/r27-chrome-journey.test.ts:
(pass) R27-W3 the chrome journey (the R26 lifecycle inside the corpus chrome) > where-to-watch reads as the viewing-source choice; play starts the peer copy [8.44ms]
(pass) R27-W3 the chrome journey (the R26 lifecycle inside the corpus chrome) > the lifecycle renders inside the chrome — Buffering(verified fraction) → Playing → seek demotion [4.17ms]
(pass) R27-W3 the chrome journey (the R26 lifecycle inside the corpus chrome) > completing → Ready offline: the earned arrival renders; the settings rows gate on truth [1.11ms]

apps/desktop/tests/r27-surface-grammar.test.ts:
(pass) R27-W3 the card grammar (pure projections) > duration labels render the corpus format; absence stays absence [0.13ms]
(pass) R27-W3 the card grammar (pure projections) > the feed card renders real artwork, pills, and honest meta [0.23ms]
(pass) R27-W3 the card grammar (pure projections) > absent artwork renders the typed placeholder — never a fabricated image [0.04ms]
(pass) R27-W3 the card grammar (pure projections) > the watched red edge carries the honest fold truth [0.04ms]
(pass) R27-W3 the card grammar (pure projections) > chips derive real facets with honest counts; the filter matches [0.12ms]
(pass) R27-W3 the card grammar (pure projections) > skeletons render the loading shells (2 text lines each) [0.06ms]
(pass) R27-W3 the surface grammar (the real composition walk) > the shell renders the honest destinations — no dead rail rows [0.64ms]
(pass) R27-W3 the surface grammar (the real composition walk) > browse renders the corpus feed: chips + real-artwork cards + skeletons [0.97ms]
(pass) R27-W3 the surface grammar (the real composition walk) > watch renders the two-column anatomy with the peer journey FIRST-CLASS [0.96ms]
(pass) R27-W3 the surface grammar (the real composition walk) > the honest lifecycle maps onto the stage state VERBATIM [0.61ms]
(pass) R27-W3 the surface grammar (the real composition walk) > search renders the row grammar — merged server + peer rows, honest statuses [1.54ms]
(pass) R27-W3 the surface grammar (the real composition walk) > library renders the rows grammar + the verified-offline note [0.94ms]
(pass) R27-W3 the shorts surface (the 9:16 grammar, the truthful read) > renders the honest empty state — never fabricated shorts rows [0.49ms]
(pass) R27-W3 the shorts surface (the 9:16 grammar, the truthful read) > projects the rows the read truthfully serves [0.04ms]

 44 pass
 0 fail
 553 expect() calls
Ran 44 tests across 4 files. [152.00ms]
