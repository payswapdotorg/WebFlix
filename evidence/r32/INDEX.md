# R32 — THE CORPUS-CITATION TABLE (surface → G4-CORPUS.md section → code seam)

The binding grammar: `docs/parity-lab/r30/gap-captures/20260926-093102/G4-CORPUS.md`
(the fourth-gap corpus, captured live 2026-09-26 09:31Z — the media gate
lifted, readyState 4 / playing). Every row: the captured grammar (cited),
WebFlix's honest binding (the code seam, cited), and the proof.

| # | Corpus surface (G4-CORPUS.md section) | The captured grammar | WebFlix's binding (code seam) | Proof |
|---|---|---|---|---|
| 1 | THE ACTION RAIL — the right vertical column (REEL-ACTION-BAR-VIEW-MODEL) | Container 48×360 at the player's right edge; 4 buttons 48×48 at a 78px vertical pitch; count labels under the icons | `ShortsFeed.tsx` `renderElements` card case → the `.wfx-shortrail` column (48px wide, right-edge/bottom anchor — WebFlix's 420px-scale viewport; the row-local grammar binds: 48px column / 48×48 buttons / 78px cell height / 24px icons per g4-rail-full.json's yt-icon measure); `globals.css` `.wfx-shortrail*` | browser-01 + probe 01 + the geometry tests |
| 2 | THE ACTION RAIL — button 1: LIKE ("like this video along with 176 thousand other people", count "176K") | like → WebFlix's like machinery; counts bind only to real data | THE EXISTING like/save machinery through the REAL actionStates seam: the frozen tree's `action:like:${itemId}` element → `fireAction` → `POST /api/actions` → the `shortsSessionReducer` (optimistic + receipt-truth + rollback). Count label: WebFlix carries NO like-count datum on this seam (the receipt settles a status, never a count) — the ICON-ONLY form (never a fabricated "176K"). In the current real boot the cards carry `capabilities: []` (host/shorts.ts — the runtime's search hits carry no capability claim) — the control is TYPED-ABSENT (the frozen tree's law) | probe 02 (the synthetic like-capable card — the icon-only cells + the a11y verbatim) + the actionStates reducer tests + the /api/actions receipt test |
| 3 | THE ACTION RAIL — button 2: COMMENTS ("View 512 comments", count "512") | comments | WebFlix has NO comments surface (the R28 comments law: WebFlix never fabricates a social surface) — the control renders NO element (omitted — the typed absence) + `data-wfx-shortrail-absent="comments remix"` carries the absence as typed data | the absence test + browser-01 (no comment control) |
| 4 | THE ACTION RAIL — button 3: SHARE (aria "Share"; the label renders as the "Share" text, no count) | share → the text form | The frozen tree's always-present share element (event-only — the tree's own law) rendered in its captured form: the 48×48 button + the "Share" TEXT in the count slot | browser-01 (the live "Share" text) + probe 01 + the share-event POST proven live |
| 5 | THE ACTION RAIL — button 4: REMIX ("Remix this Short along with 12 other remixes", count "12") | remix | WebFlix has NO remix analog — omitted (the typed absence; the frozen UserAction vocabulary has "transform" but no shorts-remix surface — no dead imitation) | the absence test + ledger row |
| 6 | THE ACTION RAIL — the channel avatar (the 5th element below remix; a small circular channel avatar that links to the channel) | avatar → the monogram law | THE MONOGRAM LAW (the rail subscriptions' established 24×24 pattern — RailSubscriptions.tsx): the channel identity's own first mark, 24×24 circular, BELOW the actions. NO LINK: WebFlix has no channel destination (the honest absence — never a fabricated link) | browser-01 (the 24×24 "F" avatar) + the avatar test |
| 7 | THE CHANNEL ROW (bottom-left) — the @handle ("@JackieMcReY") | the channel identity | THE SOURCES-MODEL IDENTITY (the N29 truth — the same field the watch page's ChannelRow reads): the connector's displayName, else the connector id. NEVER a fabricated "@handle" (WebFlix's sources carry display names, not handles). Carried in the boot payload (`host/shorts.ts` `sourceNames` — one `sources.refresh()` read) | the channel-name test + browser-01 |
| 8 | THE CHANNEL ROW — THE SUBSCRIBE PILL (aria "Subscribe to @JackieMcReY.", text "Subscribe", 78×32) | the REAL subscribe seam | `ShortsSubscribeRow.tsx` — the pill (`.wfx-subscribe--shorts`, height 32 / min-width 78 / radius 16 — the captured rect) wired to the REAL subscribe seam: `POST /api/library {op, itemId, title, connectorId, externalRef, listName: "Subscriptions"}` — the SAME write the watch page's Subscribe pill, the rail subscriptions, and the subscriptions feed use. The initial truth: the boot payload's `subscriptions` (the watch page's dual law — the stored rows by SOURCE identity [the durable cross-load key] + the hydrated local fold's item ids), kept current by the pill's writes (the session record) | browser-02 (the live subscribe round trip + the durable reload) + the subscribe/reload tests + probe 03 |
| 9 | THE CHANNEL ROW — THE TITLE + HASHTAGS (the h1 "We're sharing, right?" + "#marvel #edit #shorts") | the title → the item's own | The item's OWN overlay title (the established h2 anatomy — `element.overlayTitle` from the frozen tree). HASHTAGS: WebFlix's item model (`EntertainmentItem`: id/canonicalType/canonicalTitle/durationMs/orientation) carries NO hashtag datum — ABSENT, never fabricated tags | the title test + browser-01 |
| 10 | THE NAVIGATION ARROWS (Previous/Next, 56×56 at the right edge) | prev/next → the shorts queue navigation IF the real queue exists | The REAL queue navigation EXISTS (the existing `wfx-shorts__nav` Next/Back buttons + swipe + arrow keys — one frozen presenter) and stays AS-IS: the seam law (no nav rebuild). The captured 56×56 icon-arrow form is recorded as the honest divergence | the nav test + browser-01 + ledger row |
| 11 | THE TOP PLAYER CHROME (auto-hiding: pause k / mute m / CC / more / fullscreen f) | ONLY if WebFlix's player already exposes those controls on the shorts surface | WebFlix's shorts surface exposes NO player chrome (the feed renders the placeholder-art card stack — no media element; the R24-W2 parity controls are discovery controls, not player chrome; the watch surface's PlayerChrome is R27/R28's own, already corpus-bound) — NOT BUILT (do not build new player controls from this corpus) | ledger row |
| 12 | THE BINDING LAWS FOR THE BUILD (§G4) — the honest-divergence law, the count law, the §G3 pending law | the frozen laws | Honored throughout: real capabilities only; counts only from real data; §G3 (the home resume bar — the second account-state-absent record, 30 cards / zero progress / no continue shelf) stays PENDING — NOT built | DIVERGENCES.md |

## The lane's files (the code seams, complete)

- `apps/web/src/components/shorts/ShortsSubscribeRow.tsx` — NEW: the G4 channel row (the identity + the Subscribe pill through the REAL seam)
- `apps/web/src/components/shorts/ShortsFeed.tsx` — the G4 rail join: the corpus cell anatomy (renderElements' action case), the rail column + the 5th-element avatar (the card case), the channel row in the overlay, the session realization-index + the session subscribed-record
- `apps/web/src/host/shorts.ts` — the boot payload extension: `sourceNames` (the N29 sources read) + `subscriptions` (the dual-law library read)
- `apps/web/src/app/globals.css` — the G4 rail block (`.wfx-shortrail*` replaces the pre-corpus `.wfx-shortcard__actions`; the nav's `.wfx-roundbtn` untouched), the channel row (`.wfx-shortschannel*`), the 78×32 pill (`.wfx-subscribe--shorts`), the mobile 44px-law offset, the clear-screen rule extension
- `apps/web/tests/shorts-rail-parity.test.ts` — NEW: the 12 lane tests
- `apps/web/scripts/r32-rail-probe.ts` — NEW: the SSR probe (3 captures + probe-facts.json)
- `evidence/r32/` — this pack

Zero escalations: every file lives in `apps/web/**` (the lane-check + the CODEOWNERS boundaries agree — no code outside the web app).
