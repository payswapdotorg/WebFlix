# R32 — THE HONEST-DIVERGENCE LEDGER (WebFlix-real vs the G4 corpus)

Every row: the captured grammar (cited from G4-CORPUS.md,
docs/parity-lab/r30/gap-captures/20260926-093102/), WebFlix's real truth
(cited), and the honest resolution. Classes: `HD` honest-divergence
(WebFlix's real surface differs, by law) · `PEND` corpus-pending (never
built — the gap that stays pending) · `NOTE` a verification observation
recorded without changing the binding. Zero regressions: the pinned
controls hold (the R24-W2 shorts parity row + the real queue navigation +
the signed-out shell — byte-identical; the pre-existing suites green at
their R31 counts; the battery 5200/1/0 = the 5188 floor + exactly 12).

1. **[HD] The like count — "176K" — is ABSENT, and the like control itself
   is TYPED-ABSENT in the current real boot.** The captured rail's first
   button carries the count "176K" (the aria: "like this video along with
   176 thousand other people"); WebFlix's like machinery on the shorts
   surface — the EXISTING actionStates seam (the R32 packet's binding: the
   optimistic + receipt-truth + rollback reducer through POST /api/actions)
   — settles a receipt (confirmed / local-only / unsupported / failed),
   NEVER a count: there is no like-count datum on this seam, so the rail's
   like cell renders ICON-ONLY (never a fabricated "176K"). Deeper: the
   real boot's shorts cards carry `capabilities: []` (host/shorts.ts —
   "the runtime's search hits carry no capability claim — honest"), so the
   frozen tree omits the like/save controls entirely in the current boot
   (the tree's own law — a typed-absent affordance is never a greyed-out
   lie). The binding is proven with a synthetic like/save-capable card
   (probe 02 + the lane tests): the cells render when a source declares
   the capability, icon-only, through the frozen wiring. The G4 corpus's
   "WebFlix's like-style pill seam" line reconciles with the packet's
   operative phrasing as follows: WebFlix has TWO real like surfaces, each
   bound to its own established seam — the WATCH page's like/dislike split
   pill (the local `wfx-reactions-v1` transport + the provider dispatch
   when capable, R29-B) and the SHORTS feed's like/save machinery (the
   actionStates seam). This lane binds the shorts rail to the shorts
   surface's own machinery (the packet's explicit instruction); porting the
   watch page's reactions transport onto the shorts surface would be a NEW
   capability wiring outside this lane's seam (the seam law).

2. **[HD] COMMENTS — omitted (WebFlix has no comments surface).** The
   captured rail's second button ("View 512 comments", count "512") has no
   WebFlix analog: WebFlix renders no comments surface anywhere (the R28
   comments law — never a fabricated social surface). The rail renders NO
   control (omitted — the typed absence), with the absence carried as
   typed data (`data-wfx-shortrail-absent="comments remix"`). Never a dead
   imitation.

3. **[HD] REMIX — omitted (no analog).** The captured rail's fourth button
   ("Remix this Short along with 12 other remixes", count "12") has no
   WebFlix analog (the frozen UserAction vocabulary carries "transform",
   but no shorts-remix surface exists). Omitted — the same typed-absence
   law.

4. **[HD] The @handle form — WebFlix's sources carry display names, not
   handles.** The captured channel row renders "@JackieMcReY"; WebFlix's
   sources model carries `displayName` (the N29 truth — "Fake Source (TEST
   FIXTURE — never production)" in the fixtures boot), never a handle. The
   row renders the source's own display name (the connector id as the
   honest fallback) — never a fabricated "@" prefix (passing a display
   name off as a handle would be a fabricated grammar form).

5. **[HD] The rail's channel avatar carries NO LINK.** The captured 5th
   element links to the channel; WebFlix has NO channel destination (no
   channel pages exist — the subscribe keys the ITEM, the sources have no
   pages). The monogram avatar (the rail subscriptions' 24×24 pattern, the
   channel identity's own first mark) renders UNLINKED — the honest
   absence (never a fabricated link; linking it to the item's player
   surface would pass an item destination off as a channel destination).

6. **[HD] The hashtags — ABSENT (no model datum).** The captured row's
   "#marvel #edit #shorts" bind a hashtag field; WebFlix's item model
   (`EntertainmentItem`: id / canonicalType / canonicalTitle / durationMs /
   orientation — the frozen domain contract) carries NO hashtag datum.
   ABSENT, never fabricated tags (the same law as the R31 §G2 meta-line
   row: never pass a different datum off as the corpus's).

7. **[HD] The prev/next arrows — the existing real navigation stands (no
   rebuild).** The captured rail carries 56×56 icon arrows at the viewport's
   right edge (x=1359); WebFlix's REAL queue navigation already exists —
   the `wfx-shorts__nav` Next/Back round buttons + swipe + arrow keys, all
   driving the ONE frozen presenter (the bounded back-swipe law). The seam
   law (no redesign beyond the corpus join) binds the captured capability
   to the existing real navigation; the 56×56 icon-arrow FORM is recorded
   here as the divergence (the nav's own 48px round-button family with
   text labels, vertically centered right of the viewport).

8. **[HD] The top player chrome — NOT BUILT.** The captured auto-hiding
   chrome (pause k / mute m / CC / more / fullscreen f) belongs to the
   player the corpus captured; WebFlix's shorts surface renders NO player
   chrome (the feed is the placeholder-art card stack — no media element
   on this surface; the R24-W2 parity controls — speed / clear-screen /
   inline feedback / the source chip — are discovery controls, not player
   chrome). Per the packet's law ("do not build new player controls from
   this corpus — the player chrome is the R27/R28 watch-surface's own,
   already corpus-bound"), the chrome is NOT built here.

9. **[HD] The rail's anchor/scale adaptation.** The captured rail rides the
   716×716 player's right edge at x=1078 in the 1440 viewport, its column
   bottom-anchored ~28px above the player's bottom edge; WebFlix's shorts
   viewport is its own 420px-scale card (the established WFX-051 form). The
   rail keeps the established right-edge/bottom anchor (right: .5rem /
   bottom: 1rem) at WebFlix's scale — the ROW-LOCAL grammar binds (the
   48px column, the 48×48 buttons, the 78px pitch, the 24px icons, the
   count slot under the icon — all captured measures, applied verbatim).
   The captured container height (360px for 4 buttons + the avatar) is
   data-bound, not fixed: WebFlix's rail carries only the REAL controls
   (share-only in the current boot), so the column is shorter — the
   honest-rail law (never dead filler buttons to match a captured height).

10. **[HD] The subscribe pill's subscribed-state form.** The captured pill
    shows only the idle "Subscribe" form (78×32, white on the video);
    WebFlix's pill carries the wfx-subscribe family's own two-state
    vocabulary (idle "Subscribe" / on-state "✓ Subscribed" — the same
    words the watch page's pill renders, the established family). The
    pill's 78×32-class GEOMETRY binds (height 32 / min-width 78 / radius
    16); the on-state form is the family's own.

11. **[PEND] §G3 — the home-surface resume bar (NOT built).** The G4
    window's second account-state-absent record stands (g3-home-progress:
    30 cards, zero progress bars, no continue-watching shelf — nothing to
    bind). This lane built ONLY the shorts action rail.

12. **[NOTE] The session subscribe-record's honest scope.** The pill's
    initial truth reads the boot payload's library state (the page-load
    server read — the same class as the watch page's per-load read) and is
    kept current by THIS session's writes (the session record + the
    realization index cover the re-rank loop's fresh cards, joined by the
    durable source identity). A subscribe performed on ANOTHER surface in
    ANOTHER tab during this session is not pushed into this page's live
    state — the same staleness class every load-time read carries (the
    watch page's own pill re-reads only on its own page load). Recorded
    here because the shorts feed is a long-lived client session, not a
    per-navigation load.

13. **[NOTE] The VLM read of browser-01 read the title glyph as "Noam
    R…"** — a low-contrast misread of "Neon Rain" (the DOM truth carries
    the title; the live eval + the tests assert the item's own title).
    Recorded per the corpus discipline (the VLM record is quoted honestly,
    including its miss).
