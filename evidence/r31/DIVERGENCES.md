# R31 — THE HONEST-DIVERGENCE LEDGER (WebFlix-real vs corpus)

Every row: the corpus grammar (cited), WebFlix's real truth (cited), and the
honest resolution. Classes: `HD` honest-divergence (WebFlix's real surface
differs, by law) · `PEND` corpus-pending (never built — the two gaps that stay
pending) · `NOTE` a verification observation recorded without changing the
binding. Zero regressions: the pinned controls hold (the closed-menu state =
the trigger alone, byte-identical; the signed-out shell untouched — the
AccountMenu and the rail's Subscriptions section render only in the signed-in
chrome; the pre-existing signed-out suites green at their R30-B counts).

1. **[NOTE] §G1 — the selected-row marker: the corpus's binding finding
   followed; the re-verification observation recorded.** The binding grammar
   (GAP-CORPUS.md §G1): "a checkmark icon inside a box to its left (a
   check-in-box, not a radio dot — VLM-verified on the open picker)". This
   lane's own re-verification (two fresh VLM reads of g1-2-theme-picker.jpg +
   a pixel-level analysis of the marker region) reads the rendered marker as
   a BARE checkmark (no box outline detected; the captured HTML's content-icon
   slot carries the bare check path). The BINDING corpus finding was followed
   (the check-in-box glyph ships — `Icon.tsx`'s `checkBox`); the observation
   is recorded here per the never-build-from-memory law's own spirit. The
   corpus's actual point — the marker is a CHECK, never a radio dot — holds
   under both readings.

2. **[HD] §G1 — the picker's rows render at the account-menu panel's own
   paper-menu family, not the captured page-anchored geometry.** The corpus
   measures the rows at x=1108 in the 1440 viewport (the YouTube menu's own
   anchor); WebFlix's picker renders inside the account menu's existing
   top-right panel (the R30-B `wfx-account__panel` — the seam law: no account
   menu redesign). The ROW-LOCAL geometry binds (300×40, the 40px pitch, the
   56px label indent — the corpus's own measures); the panel's anchor is the
   established R30-B divergence (top-right anchored, ~15-20% viewport width).

3. **[HD] §G1 — the subtext's scope sentence is WebFlix's own truth, and it
   holds.** The corpus subtext ("Setting applies to this browser only")
   renders verbatim; in WebFlix the sentence is TRUE of the real seam
   (`wfx-theme` in localStorage — per-browser persistence, never an account
   claim). No divergence in the end — recorded because the corpus sentence
   was verified against WebFlix's model, not assumed.

4. **[HD] §G2 — the meta line's views/age carry NO WebFlix datum.** The
   captured meta lines ("AFTER FOOT 64K 7h ago", "Jimmy Kimmel Live 2.9M 1d
   ago") bind view counts and upload ages; WebFlix's domain model
   (EntertainmentItem: id/canonicalType/canonicalTitle/durationMs/orientation)
   carries NEITHER a view count NOR an upload age — and the stored
   subscription entry's `savedAt` is the SUBSCRIPTION's timestamp, not the
   item's upload age (rendering it as "7h ago" would pass a different datum
   off as the corpus's). ABSENT, never fabricated. The meta line's channel
   slot renders as the card grammar's own honest source identity ("From
   \<connector\>" — the same law every WebFlix card follows; the
   sources-model displayName renders on the watch surface's channel row,
   never on cards).

5. **[HD] §G2 — the "All subscriptions" layout toggle stays ABSENT.** The
   captured grid row carries the toggle (the g2 grid text + the R30 degraded
   VLM read: "a chip/toggle button labeled 'All subscriptions'"); on YouTube
   it switches the feed between the Latest grid and the All list. WebFlix has
   NO layout-toggle seam (one honest grid state exists); a toggle with one
   honest state would be a dead imitation. ABSENT (the frozen law: no dead
   imitations).

6. **[HD] §G2 — the grid-bar origin measure diverges (the shell law wins).**
   The corpus grid bar measures x=72 y=56 w=1352 in the 1440 viewport — the
   captured window ran the guide COLLAPSED to the 72px mini-rail. WebFlix's
   feed renders inside its standard shell (the frozen 240px labeled rail ≥1280
   — the seam law forbids a rail redesign), so the grid's origin is the
   shell's content area. The grid's own column law binds (the repo's browse
   law: 4 ≥1300 / 3 ≥1000 / 2 ≥600 / 1 <600 — the captured 1352px bar renders
   3 full cards + a partial 4th, the 4-column law's own geometry).

7. **[HD] §G2 — the degraded live-only state class has NO WebFlix analog
   (the conditional honestly never fires).** The corpus records the degraded
   state (R30 raw/07 + the R30 corpus §10: a "Latest" header and 3 live
   cards — red "LIVE" badges, "1.5K watching" meta). WebFlix's domain model
   carries NO liveness datum (no live concept in EntertainmentItem or any
   realization; the connector read returns title/type/duration/artwork only),
   so "when the connector truth is live-only" can never honestly hold — a
   LIVE badge or a watching count would fabricate a datum the sources do not
   carry (the R30-B divergence-ledger law: bound to the real connector
   truth, no fabricated grid elements). The grid's honest state classes:
   healthy / empty / unjoined. A future connector that carries liveness binds
   at the same join seam.

8. **[HD] §G2 — the cards are the repo's own card grammar, not a new §G2
   family.** The captured card anatomy (thumbnail, bold title, the channel
   row with the circular avatar + name, the grey meta line) maps to
   WebFlix's established ItemCard (the same card every feed surface renders
   — the seam law's minimal join): the thumbnail (+ the real artwork when
   the connector serves it), the title, the channel row ("From
   \<connector\>" — no circular avatar on the wide card variant; the card
   grammar's own form), the duration badge. The corpus card's avatar row and
   meta line land in the card grammar's own slots; the deltas are rows 4
   (views/age) and this row (the channel row's form).

9. **[HD] §G2 — the rail seam: the heading LINK is the join, and the lead's
   "already points" claim is made true by this lane.** The task records the
   destination law as "the rail's Subscriptions section already points at
   the real destination per R30-B" — at the R30-B lane head the section's
   ROWS pointed at the entries' player surfaces (the per-entry
   destinations), and the section HEADING was a plain `<h3>` (no feed
   destination existed — the honest R30-B absence, its divergence row 17).
   This lane builds the feed and joins the heading to it (ONE element: the
   heading's text becomes the `/feed/subscriptions` anchor — the corpus
   taxonomy's Subscriptions entry, the exact join class of R30-B's
   Playlists row → /library). The rows, geometry, position, and the
   signed-out rail are unchanged; flagged here so the lead re-verifies the
   seam against R30-B's actual record.

10. **[PEND] §G3 — the home-surface resume bar.** The captured account's
    home renders 30 cards with ZERO progress bars and NO continue-watching
    shelf (g3-home-progress.json) — the surface is account-state-dependent;
    nothing to bind. NOT built (the task's frozen law). WebFlix's own
    Continue-Watching grid is untouched.

11. **[PEND] §G4 — the shorts action rail.** The risk-engine gate blocks
    the shorts player itself even on the healthy window (g4-shorts.json +
    g4-shorts-scroll.json: video readyState 0, scroll does not unblock —
    the third consistent record). NOT built; WebFlix's own shorts surface
    stays untouched.

12. **[HD] The loading frame keeps the boot-state chrome.** The feed route's
    `loading.tsx` renders the shell without the account prop (the transient
    skeleton — the same accepted pattern every route's loading already has:
    the R30-B divergence row 19, unchanged). Zero-risk keep; the loaded page
    carries the request truth.

13. **[HD→VERIFIED] The browser-level probe — NOT blocked in this sandbox (the
    R30-B BLK row does not reproduce).** The dev server boots cleanly here
    (HTTP 200; the R30-B record's OOM-kill class was their sandbox's ceiling),
    so the golden paths were proven LIVE (agent-browser @1440×900): §G1's
    menu→row→picker flow with the measured 300×40/40px-pitch geometry and the
    Dark/device write-throughs, §G2's real-pill subscribe → the feed grid →
    the card's player click-through. The full record:
    `browser-verification.md`; the screenshots: `captures/browser-*.png`.
    The SSR composition captures (`captures/0*-*.html` + `probe-facts.json`)
    remain the frozen per-surface proof set (the R30-B instrument class).

## Regressions

**NONE.** The battery on the final lane tree: **5188/1/0** (the base floor
5165/1/0 — directly re-measured on the isolated base worktree — + exactly the
23 new R31 tests; the honest R11 webtorrent environment skip unchanged as the
1). The pinned controls hold — AND were verified live at the browser level:
the closed-menu state is the trigger alone (the island's initial render —
`captures/00-account-menu-closed.html`), the signed-out shell renders no
AccountMenu and no rail Subscriptions section (the AppShell's `signedIn` gate
— R30-B's own; asserted in `tests/subscriptions-feed.test.ts`'s signed-out
control + the R30-B signed-out suites, green at their counts).
