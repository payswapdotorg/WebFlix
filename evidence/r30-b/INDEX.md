# R30-B — THE CORPUS-CITATION TABLE

Lane: `wfx/r30/b` (base: `main @ f1bdba6` + `origin/wfx/r30/lead-captures @ b77873b`).
The binding corpus: `docs/parity-lab/r30/lead-captures/CORPUS.md` (+ `README.md` the
window record, `raw*/` the DOM truths, `vlm/` the visual reads). Every shipped surface
cites its section; every diverged row cites the honest reason. The four corpus-pending
gaps (README "Honest gaps") are NOT built — recorded in DIVERGENCES.md.

| Surface | CORPUS.md section | The captured grammar | The code seam | Proof |
|---|---|---|---|---|
| The masthead bell button (40x40) + the unread badge | §1 "Masthead account cluster" [measured, run-1 09:38] + "Cross-cutting truths" | "Bell button: 40x40 …; unread badge text '9+' (the badge caps at 9+ while the title count read 167)" | `components/shell/bell-grammar.ts` (the TWO layers: `bellBadgeText` the cap — 1-9 verbatim, 10+ "9+", 0 paints nothing; `documentTitleWithCount` the true count in the title) + `components/shell/MastheadBell.tsx` (the 40x40 `wfx-topbar__guide` shell + the badge paint + the title-layer effect) | `tests/account-chrome.test.ts` (the 167/"9+"/"(167)" pairs, verbatim); `captures/01-masthead-signed-in.html` (the honest zero: no badge) |
| The masthead bell panel | §2 "Notifications bell panel" [rendered, run-1 02-bell-panel.jpg] | "Panel: anchored top-right under the bell; ~360-400px wide … Header: bold 'Notifications' + a settings gear icon + a collapse/close arrow" + the row anatomy | `MastheadBell.tsx` → `BellPanelContent` (the `wfx-bellpanel` 400px top-right paper-menu + the header + gear + collapse; the body = the HONEST-EMPTY row — the capability row's frozen empty state; the row anatomy NEVER renders: no notification source, never a fabricated row) | `tests/account-chrome.test.ts` (§2 describe); `captures/03-bell-panel.html` |
| The avatar squircle + the account-menu trigger | §1 [rendered] ("the rendered avatar image is a squircle/rounded shape, not a circle") | — | `components/shell/AccountMenu.tsx` (the `wfx-account__triggeravatar` squircle chip, r12 on 32px — the corpus shape; the radius itself not corpus-measured) | `tests/account-chrome.test.ts`; `captures/01` + `captures/04` |
| The account menu (the 14-row grammar) | §3 "Account menu (avatar dropdown)" [rendered, DOUBLE-CONFIRMED] | The header (name/handle/"View your channel") + rows 1-14 with state-bearing labels | `AccountMenu.tsx` → `AccountMenuPanel` (the header: the name + the EMAIL identity line + [no channel — absent]; rows 2/3/6/7/8/11/12 REAL — Switch account via `PUT /api/auth/select-profile`, Sign out via `POST /api/auth/logout`, Your data → `/settings?section=general`, `Appearance: <state>` the theme seam's truth, `Display language: <state>` the session's real locale via `Intl.DisplayNames`, Keyboard shortcuts → the shared real key sheet, Settings → `/settings`; rows 1/4/5/9/10/13/14 + "View your channel" named in the absence note — never dead imitations) | `tests/account-chrome.test.ts` (the §3 describes); `captures/04-account-menu.html` |
| The masthead end cluster (the logged-in state) | §1 [measured+rendered] ("End cluster: 225x40 … order from search rightward: mic (in-bar), Create, Bell, Avatar") | — | `components/shell/AppShell.tsx` (the `account` prop, signed-in: Create + gear + `MastheadBell` + `AccountMenu`; the sign-in pill = the LOGGED-OUT variant — replaced, per the corpus; the gear kept: WebFlix's own R29-verified surface — see DIVERGENCES) | `tests/account-chrome.test.ts` (the signed-in-shell describe); `captures/01` vs `captures/02` (the signed-out control) |
| The rail subscriptions section | §4 "Subscriptions rail (guide)" [measured, run-2 09:45 + rendered] | "Entry geometry: 204x40 per guide entry. Channel avatars in the rail: 24x24 rendered … flat list, avatars left" | `components/shell/RailSubscriptions.tsx` (the `wfx-railsub` section between primary and You; the 24x24 monogram avatar — the sources carry no channel photos; the 204x40 `wfx-railsub__row`; each row its own real player href; unjoined → UNLINKED) + `globals.css` (the corpus 204x40 guide-entry box on the labeled/drawer rail forms) | `tests/account-chrome.test.ts` (the §4 describes — the stored-truth flow through the REAL `POST /api/library` subscribe seam); `captures/05-rail-subscriptions.html` |
| The rail section taxonomy | §4 (the exact order: [Home, Shorts] / [Subscriptions] / [You…] / [More from YT] / [Explore] / [Report history]) | — | `AppShell.tsx` (the Subscriptions section joins where the corpus places it; the You group gains the Playlists row → `/library` — the real destination; More-from-YT/Explore/Report-history honestly absent — the divergence-5 law) | `tests/account-chrome.test.ts`; `captures/01` |
| The history resume-bar | §6 "Watch history page" [rendered] ("The watched-progress red bar renders in the row thumbnail area — the N19[P] observed instance") | — | The pre-existing `ItemCard` resume grammar (unchanged — the seam law) bound to the history fold's stored truth; the §6 test + capture verify the binding | `tests/playlist-family.test.ts` (the §6 describe); `captures/06` |
| The playlist header grammar + the pills | §8 "Watch later playlist page" [measured + rendered] | "Header panel: bold 'Watch later' · owner name · '99 videos' · 'No views' · 'Last updated on Sep 16, 2026' · two pill buttons: 'Play all' (white) + 'Shuffle' (dark)" | `components/library/LibrarySurface.tsx` (the per-list header: title · the session's own owner truth · "N videos" · "Last updated on MMM D, YYYY" via `formatPlaylistDate`) + `components/library/PlaylistControls.tsx` (Play all + Shuffle — the REAL queue seam: POST /api/queue per entry + navigation to the first entry's player href) | `tests/playlist-family.test.ts` (the §8 describes + the queue-seam round trip); `captures/06` |
| The honest-unavailable notice | §9 "Liked videos playlist page" [rendered] ("Grey banner: '6 unavailable videos are hidden'") | — | `LibrarySurface.tsx` (the playlist sections' entries with NO joined source identity hide behind the counted notice — plural-honest) | `tests/playlist-family.test.ts` (the §9 unavailable describe); `captures/06` |
| The sort chips | §9 [rendered] ("Sort chips: All (selected) · Videos · Shorts") | — | `PlaylistControls.tsx` (All + the REAL types the list carries — the ChipBar law; the CSS filter seam `[data-wfx-playlist-filter]` on the section root; the rows carry `data-wfx-playlist-type`) | `tests/playlist-family.test.ts` (the chip describes); `captures/06` |
| The keyboard shortcuts sheet (the account menu's row 11) | §3 row 11 | — | `components/shell/ShortcutsSheet.tsx` (the gear's own real key set, extracted verbatim — ONE truth, two entries) | `tests/account-chrome.test.ts` (row 11) |

## The corpus-citation coverage

- **SHIPPED (bound, cited)**: §1 (bell geometry + the two-layer badge/title grammar + the avatar squircle + the end-cluster swap), §2 (the panel anatomy + the honest-empty row), §3 (the 14-row grammar — 7 real rows, 7+1 absent named), §4 (the flat list + 24x24 avatars + 204x40 entries + the taxonomy join), §6 (the resume-bar binding verified), §8 (the header grammar + Play all/Shuffle through the real queue), §9 (the unavailable notice + the real-type chips).
- **DIVERGED (honest rows — see DIVERGENCES.md)**: §1's mic (no voice transport — honestly absent), §1's end-cluster order (the gear kept), §3's handle (the email is the real identity datum) + "View your channel" (no channel surface), §7 (the playlists GRID grammar — WebFlix's playlists are Library sections; no privacy/course/view-full analogs), §8's "No views" (no datum) + the row index grammar (the rows keep the watchlist grammar — the seam law), §10 (the subs feed — WebFlix's subscriptions surface is the rail + the Library list, bound to the connector truth; no fabricated grid).
- **PENDING (never built — the README's four gaps)**: the theme-picker submenu rows (only the "Appearance: Device theme" state label is captured), the home-surface resume bar, the healthy subscriptions-feed grid, the shorts action rail (§11 — the gate still active logged-in; WebFlix's own shorts surface untouched).

The full gate battery: `guards.md`. The per-surface captures: `captures/` + `probe-facts.json`
(generated by `apps/web/scripts/r30-account-chrome-probe.ts` — the SSR composition probe;
the browser-level probe is blocked in this sandbox, recorded in DIVERGENCES.md).
