# R30-B — THE HONEST-DIVERGENCE LEDGER (WebFlix-real vs corpus)

Every row: the corpus grammar (cited), WebFlix's real truth (cited), and the honest
resolution. Classes: `HD` honest-divergence (WebFlix's real surface differs, by law) ·
`PEND` corpus-pending (never built — the README's four gaps) · `BLK` sandbox-blocked
(the proof form, not the surface). Zero regressions: the signed-out shell is
byte-identical to the R29-verified chrome (`tests/account-chrome.test.ts` — the
signed-out describes; `captures/02`).

1. **[HD] §1 — the masthead mic is honestly ABSENT.** The corpus logged-in window HAS
   a mic (40x40, r50, in-bar — "the logged-in corpus HAS a mic; the logged-out
   masthead census had none — D3/N1 divergence note"). WebFlix carries NO voice-search
   transport (the R29 sweep's divergence 10: `getUserMediaCalls: 0` — no decorative
   mic shipped, none claimed). Per the task's own law ("no real transport =
   honestly-absent or the real one, never decorative"): ABSENT, unchanged.

2. **[HD] §1 — the end-cluster order: the settings GEAR stays in the signed-in
   cluster.** The corpus end cluster (mic, Create, Bell, Avatar — 225x40) carries no
   gear; WebFlix's gear is the R29-B N24 verified surface (the corpus multi-page
   menu). The seam law (no masthead redesign) keeps it; the divergence is the keep.

3. **[HD] §1 — the avatar trigger keeps WebFlix's ≥44px touch floor.** The corpus
   measured the YouTube avatar button rect at 54x34; WebFlix's frozen a11y law
   (≥44px targets) wins — the 32px squircle chip renders inside a 44px trigger.

4. **[HD] §1 — the avatar is the monogram, not a photo.** The corpus avatar is a real
   account photo; WebFlix's sources carry no account photos — the monogram squircle
   is the honest stand-in (the channel row's own law).

5. **[HD] §3 — the identity line is the account's EMAIL, not a handle.** The corpus
   header carries a handle ("@spartacus_payswap"); WebFlix's account model has no
   handle concept — the email is the real identity datum (`RequestSessionView.account.email`,
   the persona's `dev@webflix.local` in dev). Never a fabricated handle.

6. **[HD] §3 — "View your channel" is absent (no channel surface).** WebFlix content
   arrives through connected sources; no channel surface exists. Named in the menu's
   own absence note.

7. **[HD] §3 — 7 of the 14 corpus rows are honestly absent** (Google Account,
   YouTube Studio, Purchases and memberships, Restricted Mode, Location, Help, Send
   feedback): no account-management page beyond Settings, no studio, no purchases
   surface, no content-restriction engine (an "Off" state would fabricate an engine),
   no location signal (never a fabricated "Hong Kong"), no help surface, no
   user-feedback transport (the feedback fixtures are recommendation controls, not a
   developer-feedback channel). The rows are named in the menu's own absence note —
   the gear's established pattern, never dead imitations.

8. **[HD] §3 — the theme-picker submenu rows stay CORPUS-PENDING.** The README's
   first honest gap: the picker rows were never captured open; only the
   "Appearance: Device theme" state label is (double-confirmed). WebFlix renders the
   state-bearing row (the real theme-seam truth: "Device theme" with no stored
   choice — WebFlix's own boot law; "Dark theme"/"Light theme" when persisted) with
   NO picker behind it — never built from memory.

9. **[HD] §3 — "Your data in YouTube" renders as "Your data in WebFlix"** (the
   product-name swap — the identity law: our name, YouTube's placement; the R29-B
   gear's own row label family).

10. **[HD] §4 — the rail subscriptions are the ITEMS the Subscribe pill saves, not
    channel identities.** WebFlix's subscribe write keys the canonical item (the
    frozen `SUBSCRIPTIONS_LIST` named-list write); the corpus rail lists channels.
    The honest mapping: the flat list renders the stored Subscriptions entries —
    each entry's own title (never a fabricated channel name), the row's destination
    its own player surface. The 24x24 monogram avatar (the sources carry no channel
    photos) carries the corpus's measured avatar grammar.

11. **[HD] §4 — the rail subscriptions read the library surfaces' own truth (the
    anonymous singleton's fold), not the signed-in identity's profile library.** The
    one-runtime-at-boot law: the Subscribe writes (`POST /api/library`) and the
    Library page both flow through the singleton; the rail reads the SAME fold (the
    R30-A `hydrate()` seam — the stored truth, memoized per engine). The
    identity-scoped profile library stays the Settings surface's scope.

12. **[HD] §4 — "Your channel", More-from-YouTube, Explore, Report history stay
    absent** (the R29 sweep divergence-5 law, unchanged: no real backing on this
    host). The corpus taxonomy's REAL rows join: the Subscriptions section (§4),
    the You group's History + Playlists rows (Playlists → `/library` — the real
    destination where the playlists render; no separate playlists route exists).

13. **[HD] §6 — the corpus's right-rail history controls + type chips (Search
    watch history / Clear all / Pause / Manage all / Comments / Posts / Live chat;
    All/Videos/Shorts/Podcasts/Music) are NOT built.** The task's build list for
    §6 binds the resume-bar grammar only ("the resume-bar grammar … on WebFlix's
    history/continue surfaces bound to the LibraryEngine's stored truth"); the
    control-rail grammar has no WebFlix backing (no history-management engine to
    wire Clear-all/Pause to — the seam law forbids dead imitations). Recorded here
    as the honest scope.

14. **[HD] §7 — the playlists GRID grammar does not ship.** WebFlix's playlists are
    the Library's named-list sections (the R24-W2 design — the seam law keeps them);
    the corpus grid's card anatomy (privacy labels, "View full playlist" links, the
    course playlists' provider + "Course" + "View full course") has no WebFlix
    analog (no privacy model, no course entities). The count line ("N videos")
    carries the grammar's real datum.

15. **[HD] §8 — the owner line is the session's own truth.** The corpus header
    carries the account name ("Spartacus"); WebFlix's Library pages render through
    the anonymous host singleton by design (the page-level session truth: "an
    anonymous session" in the default boot, the account label when the host binds
    one). "No views" stays absent (no playlist-views datum — never fabricated).
    The rows keep the watchlist row grammar (no index numbers — the seam law's
    no-Library-row-redesign).

16. **[PEND] §5 — the home-surface resume bar.** The corpus has NO resume bar on
    any home card (measured + rendered agree); WebFlix's pre-existing
    Continue-Watching grid is its own surface (untouched — never built from this
    corpus). Stays pending.

17. **[PEND] §10 — the healthy subscriptions-feed grid.** Only the degraded
    live-only state is captured; WebFlix's subscriptions surface stays bound to its
    real connector truth (the rail section + the Library's Subscriptions list). No
    fabricated grid.

18. **[PEND] §11 — the shorts action rail.** The gate is STILL ACTIVE logged-in
    (shell + Play CTA only, NO action rail); WebFlix's own shorts surface (the real
    element tree with real actions) is untouched — nothing built from this corpus.

19. **[HD] The loading frames keep the boot-state chrome.** The route `loading.tsx`
    files render the shell without the account prop (the transient skeleton — the
    same accepted pattern the Settings page's loading already has: the loaded page
    carries the request truth). Zero-risk keep; the pages all wire the corpus
    chrome.

20. **[BLK] The browser-level probe.** The dev server is OOM-killed at compile in
    this sandbox (the process dies on the first request's compile — no error line,
    the same memory-ceiling class the R30-A lead recorded for the boot-B probe).
    The seams are TEST-proven (23 tests, the battery's +23) and frozen as SSR
    composition captures (`captures/` — `apps/web/scripts/r30-account-chrome-probe.ts`).

## Regressions

**NONE.** The battery: 5165/1/0 (the 5142/1/0 floor + exactly the 23 new R30-B tests;
the honest R11 webtorrent environment skip unchanged as the 1). The signed-out shell
is byte-identical (`tests/account-chrome.test.ts` — the signed-out-control describes;
the pre-existing AppShell-rendering suites green at their R29/R30-A counts).
